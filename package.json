{
  "name": "navi",
  "version": "0.0.5",
  "scripts": {
    "start": "php -S 'localhost:8000' & open http://localhost:8000 & grunt watch",
    "stop": "ps aux | grep localhost | awk '{print $2}' | xargs -I {pid} kill -9 {pid}"
  },
  "devDependencies": {
    "grunt": "^1.0.4",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-cssmin": "^3.0.0",
    "grunt-contrib-sass": "^0.9.2",
    "grunt-contrib-watch": "^1.1.0"
  },
  "dependencies": {
    "bootstrap": "^4.5.0",
    "gem": "^2.4.3",
    "highlight.js": "^9.15.10"
  }
}
